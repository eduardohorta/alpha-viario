# Base bruta de sinistros (~15 MB, reproduzível via scripts/processar_sinistros_distancia.py)
dados/brutos/cat_acidentes.csv

# Revisões internas (CODEX) — bastidores não publicados
revisoes/

# macOS / LaTeX
.DS_Store
*.aux
*.log
*.toc
*.out

# Respostas brutas do questionário (observação livre pode conter dado pessoal)
# e resumo gerado (a comissão decide o que publicar). O modelo é versionado.
consultas/respostas/respostas.csv
consultas/respostas/resumo-respostas.md

# Documento privado (termo de compromisso) — não publicar
TC Alphaville.pdf

# Análise interna (termo de compromisso etc.) — não publicar
interno/

/expedientes-unicos
